gestion des expansions:
expansion len ne gere plus que la len quand les vars a etendre sont etendues
expansion.c n expand que les var et celle qui sont entre double quotes
expansion split permet de split selon les espaces en prenant en compte les quotes
TEST expansion split

comment ca s'agence ?
= split selon les |
= expand les variables
= split expand selon les ' '
= fonction qui recup les redirection en fonction de si c'est dans deux str ou une seule (<, name OU <name)
= fonction qui compte le nombre d'arg et les met dans un char **
= fonction qui cree les tokens de redirection (AAAARGH)